# retail-intelligence-core
End-to-end retail intelligence pipeline combining time-aware sales demand forecasting (XGBoost, Ridge, Prophet), store behavioral clustering, and neural POS return anomaly detection with an interactive Streamlit dashboard.
